# boardid.config

# TODO: pull in whatever Phytec patch sets the serial number and maybe
#       that doesn't even apply to GRiSP 2
#-b cpuinfo -n 4

# Use the MAC address of the first Ethernet port
-b macaddr -n 4
